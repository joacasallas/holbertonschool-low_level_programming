#include "main.h"

/**
 * add - adds two integers
 * @a: first integer to sum
 * @b: second integer to sum
 * Return: result sum.
 */

int add(int a, int b)
{
	int sum = 0;

	sum = a + b;
	return (sum);
}
